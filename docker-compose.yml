services:
  job-analytics-dashboard:
    build: .
    container_name: job-analytics-dashboard
    env_file:
      - .env.local
    # Bound to loopback so the app is only reachable through nginx/TLS.
    ports:
      - "127.0.0.1:4000:4000"
    restart: unless-stopped
